// DELETE THIS FILE.
//
// A scratch driver used while building the stability report and the `stableSamples` sweep. It is
// empty of behaviour now; everything it did lives in `test/stability.test.ts`,
// `test/settle-sweep.test.ts` and `src/stability-cli.ts`. The unit that wrote it could not remove
// it (`rm` and `mv` are denied to this agent), so it is recorded here rather than left to be
// discovered - the same note `probe.ts` in this package's root already carries, for the same
// reason.
export {};
