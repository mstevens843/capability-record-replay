// DELETE THIS FILE.
//
// It was a throwaway driver used to re-run the terminal and combined kill matrices while
// regenerating `docs/design/FINAL-STATUS.md`. `rm` is denied to every agent in this tree, so it
// could not be removed; its body was emptied instead so that `pnpm lint` stays green. The runnable
// copy lives at `.scratch/corpora.mts`, which is gitignored.
//
//   rm packages/conformance/.corpora.mts

export {};
