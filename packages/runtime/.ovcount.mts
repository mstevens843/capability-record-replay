// DELETE THIS FILE.
//
// A throwaway driver written while regenerating `docs/design/FINAL-STATUS.md`, to count the summit
// overlay's vocabulary overrides. It did not run; the counts in that document were taken from the
// source text instead. `rm` is denied to every agent in this tree, so the body was emptied to keep
// `pnpm lint` green.
//
//   rm packages/runtime/.ovcount.mts

export {};
